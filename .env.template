KIS_APP_KEY=str
KIS_APP_SECRET=str
KIS_ACCOUNT_NO="12345678-01"
KIS_HTS_ID=str  # 실시간 체결통보 구독에 필요한 HTS 로그인 ID
IS_MOCK=True   # 기본값은 모의투자

# 실전투자(IS_MOCK=False)로 전환할 때만 true로 설정. 둘 다 만족해야 실거래 진입.
I_UNDERSTAND_REAL_MONEY_RISK=False

# --- 리스크 관리 (선택, 기본값 있음) ---
# REBALANCE_BAND_PCT=0.05
# MAX_POSITION_PCT=0.30
# MAX_ORDER_NOTIONAL_KRW=1000000
# MAX_DAILY_LOSS_PCT=0.03
# ORDER_COOLDOWN_SEC=1800
# WS_STALENESS_THRESHOLD_SEC=120

# --- LLM (뉴스 감성분석 / 목표비중 제안) ---
LLM_PROVIDER=gemini
GEMINI_API_KEY=str
# GEMINI_MODEL=gemini-3.6-flash