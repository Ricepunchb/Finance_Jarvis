KIS_APP_KEY=str
KIS_APP_SECRET=str
KIS_ACCOUNT_NO="12345678-01"
KIS_HTS_ID=str  # 실시간 체결통보 구독에 필요한 HTS 로그인 ID
IS_MOCK=True   # 기본값은 모의투자

# 실전투자(IS_MOCK=False)로 전환할 때만 true로 설정. 둘 다 만족해야 실거래 진입.
I_UNDERSTAND_REAL_MONEY_RISK=False

# --- 리스크 관리 (선택, 기본값 있음) ---
# REBALANCE_BAND_PCT=0.05
# MAX_POSITION_PCT=0.30
# MAX_ORDER_NOTIONAL_KRW=1000000
# MAX_DAILY_LOSS_PCT=0.03
# ORDER_COOLDOWN_SEC=1800
# WS_STALENESS_THRESHOLD_SEC=120

# --- 손절/트레일링익절/스윙 시그널 (선택, 기본값 있음) ---
# STOP_LOSS_PCT=0.07
# TRAILING_TAKE_PROFIT_PCT=0.05
# INTRADAY_BAR_MINUTES=30
# INTRADAY_LOOKBACK_CALENDAR_DAYS=20
# INTRADAY_BACKFILL_SYMBOLS_PER_CYCLE=3

# --- 펀더멘털 밸류에이션 (선택, 기본 꺼짐) ---
# ENABLE_FUNDAMENTAL_VALUATION=True
# VALUATION_CACHE_TTL_HOURS=24
# DART_API_KEY=str  # opendart.fss.or.kr 무료 가입 후 발급 (Phase 2, 현재 미사용)

# --- LLM (뉴스 감성분석 / 목표비중 제안) ---
LLM_PROVIDER=gemini
GEMINI_API_KEY=str
# GEMINI_MODEL=gemini-3.6-flash
# 주 모델이 5xx(수요 폭주 등)를 반환하면 이 모델로 한 번 더 시도한다. 빈 문자열이면 폴백 없음.
# GEMINI_FALLBACK_MODEL=gemini-2.5-flash

# --- AI 포트폴리오 에이전트 (선택, 기본값 있음 — 재비중 제안의 안전 범위) ---
# AI_REBALANCE_MAX_TURNOVER_PCT=0.20
# AI_REBALANCE_MAX_WEIGHT_DELTA_PCT=0.10
# AI_REBALANCE_MAX_SYMBOLS_ADDED=2
# AI_REBALANCE_MAX_SYMBOLS_REMOVED=2
# AI_REBALANCE_MIN_SYMBOL_WEIGHT_PCT=0.03
# AI_REBALANCE_MAX_PORTFOLIO_SYMBOLS=8

# --- AI 포트폴리오 에이전트 - 종목 발굴 (선택, 기본값 있음) ---
# DISCOVERY_TOP_N=15

# --- AI 포트폴리오 에이전트 - 스케줄러 (선택, 기본값 있음) ---
# 스케줄러 자체는 /ai-rebalance/scheduler API로 켜야 동작한다(기본 꺼짐).
# AI_REBALANCE_MIN_INTERVAL_SEC=86400
# AI_REBALANCE_PERIODIC_INTERVAL_DAYS=30
# AI_REBALANCE_DRIFT_TRIGGER_BUFFER_PCT=0.05
# AI_REBALANCE_NEWS_TRIGGER_STRENGTH=0.7